#!/usr/bin/env bash
set -Eeuo pipefail

selection="$(cliphist list | fuzzel --dmenu --prompt='Clipboard > ' --width=80 --lines=16)" || exit 0
[[ -n "$selection" ]] || exit 0
printf '%s\n' "$selection" | cliphist decode | wl-copy
